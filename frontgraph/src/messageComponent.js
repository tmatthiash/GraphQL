import React, { Component } from 'react';

import Messages from './messageQuery';

class MessageComponent extends Component {
  render() {
    return <Messages />;
  }
}

export default MessageComponent;